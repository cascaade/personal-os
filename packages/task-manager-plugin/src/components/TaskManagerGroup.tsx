import { Commitment } from "@/services/CommitmentsProvider";
import { memo } from "react";

function TaskManagerGroup({commitments}: {commitments: Commitment[]}) {
    return (<></>)
}

export default memo(TaskManagerGroup);
